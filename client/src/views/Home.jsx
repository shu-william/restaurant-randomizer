import React, { useState } from 'react';
import axios from 'axios';

const Home = (props) => {

    const [fetchedData, setFetchedData] = useState({});

    return (
        <div>
            
        </div>
    )
}

export default Home;
